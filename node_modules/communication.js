const communication = {
	sendAll: (game, payload) =>
	{
		//console.log(game);
		game.clients.forEach (c => {
			clients[c.clientId].connection.send(JSON.stringify(payload));
		})
	},
	chat: (game, clientId, nick, message) =>
	{
		const payload = {
			"method" : "chatmsg",
			"clientId": clientId, 
			"nick": nick,
			"message": message
		}
		communication.sendAll(game, payload)
	}
}	
module.exports = communication