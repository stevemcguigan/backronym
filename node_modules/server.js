const communication = require('communication')
const server = {
	getGame: (client, games) =>
	{
		const payload = {
			"method": "getGames",
			"games" : games
		}
		communication.send(payload);			
	}
}
module.exports = server